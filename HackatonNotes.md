# The problem

# Questions
- La velocità iniziale è nota ?
- Il server conosce la speed formula ?
- La formula è random ? Quando viene generata ? è uguale per tutti i player ?

# Idea
- Diffie hellman car parameters generation (and store on smart contract the parameters)