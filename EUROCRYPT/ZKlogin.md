ZKA
Use ZK proof to attest something (that a statement is true).
INstead of asking the user to reveal the document, state something and generate a proof.
Issued by an authority, document should be verifiable.
What do we have to prove:
- Statement exists;
- Exist at the correct place;
- Conforms to a grammar (like json);
- ecc;

ZKLogin: protocol to allow user to authorize transaction in a blockchain.
Via ZKP of a JWT signed token.
